# No secrets are stored in repository files.
# API keys are stored in the system credential storage (macOS Keychain).
# Example environment overrides (optional):
# IDLE_CUA_DATA_DIR=~/.idle-cua
# OPENROUTER_API_KEY= # do not put real keys here
